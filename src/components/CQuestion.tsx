import React from 'react';
/*
function CQuestion(props: { content: React.ReactNode; }) {
  return (
    <h2 className="question">{props.content}</h2>
  );
}
CQuestion.propTypes = {
  content: React.PropTypes.string.isRequired
};
export default CQuestion;
*/
